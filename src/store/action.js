export const USER_NAME_ACTION = (name) => {
  return {
    type: 'USER_NAME',
    name
  }
};

export const LOGIN_INFO_ACTION = (loginInfo) => {
  return {
    type: 'LOGIN_INFO',
    loginInfo
  }
};